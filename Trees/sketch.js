
function setup() {
  createCanvas(window.innerWidth,window.innerHeight);
  background(0);
  tree(window.innerWidth/2,window.innerHeight,0,-2,0,50);
}
function draw() {
}

function tree(x,y,xz,yz,i,branchlength) {
  fill()
}
